namespace EasyLog.Enums;

/// <summary>
/// Enumeration specifying the format in which log entries are serialized and persisted.
/// </summary>
public enum LogFormat
{
    /// <summary>
    /// Logs are formatted and stored in JSON format.
    /// </summary>
    JSON,
    /// <summary>
    /// Logs are formatted and stored in XML format.
    /// </summary>
    XML
}
